import PaymentFormContainer from './PaymentFormContainer';
import PaymentConfirm from './PaymentConfirm';

const Payment = {
  Form: PaymentFormContainer,
  Confirm: PaymentConfirm,
};

export default Payment;
